<div class="weather-card">
  <div class="card-glow"></div>
  <div class="weather-header">
    <h2 class="weather-title">
      <i class="ri-cloud-sun-line title-icon"></i>
      天气预报
    </h2>
  </div>
  
  <div class="weather-content">
    <div id="weather-display" class="weather-display">
      <div class="weather-loading">
        <div class="loading-spinner"></div>
        <p>正在获取天气...</p>
      </div>
    </div>
  </div>
</div>

<script>
  (function() {
    const weatherDisplay = document.getElementById('weather-display');
    
    // 使用浏览器地理位置获取天气
    if (navigator.geolocation) {
      navigator.geolocation.getCurrentPosition(
        (position) => {
          const lat = position.coords.latitude;
          const lon = position.coords.longitude;
          fetchWeatherData(lat, lon);
        },
        (error) => {
          // 默认显示北京天气
          fetchWeatherData(39.9042, 116.4074, '北京');
        }
      );
    } else {
      fetchWeatherData(39.9042, 116.4074, '北京');
    }

    async function fetchWeatherData(lat, lon, cityName = null) {
      try {
        // 使用Open-Meteo免费API（无需密钥）
        const response = await fetch(`https://api.open-meteo.com/v1/forecast?latitude=${lat}&longitude=${lon}&current_weather=true&daily=temperature_2m_max,temperature_2m_min,weathercode&timezone=auto`);
        const data = await response.json();
        
        const current = data.current_weather;
        const daily = data.daily;
        const weatherCode = current.weathercode;
        
        // 天气代码映射
        const weatherIcons = {
          0: 'ri-sun-line', 1: 'ri-cloud-sun-line', 2: 'ri-cloud-sun-line', 3: 'ri-cloud-line',
          45: 'ri-fog-line', 48: 'ri-fog-line',
          51: 'ri-rainy-line', 53: 'ri-rainy-line', 55: 'ri-rainy-line',
          61: 'ri-rainy-line', 63: 'ri-rainy-line', 65: 'ri-rainy-line',
          71: 'ri-snow-line', 73: 'ri-snow-line', 75: 'ri-snow-line',
          95: 'ri-thunderstorm-line', 96: 'ri-thunderstorm-line', 99: 'ri-thunderstorm-line'
        };
        
        const weatherTexts = {
          0: '晴朗', 1: '多云', 2: '多云', 3: '阴天',
          45: '雾', 48: '雾',
          51: '小雨', 53: '中雨', 55: '大雨',
          61: '小雨', 63: '中雨', 65: '大雨',
          71: '小雪', 73: '中雪', 75: '大雪',
          95: '雷雨', 96: '雷暴', 99: '强雷暴'
        };
        
        const icon = weatherIcons[weatherCode] || 'ri-thermometer-line';
        const text = weatherTexts[weatherCode] || '未知';
        const location = cityName || '当前位置';
        
        weatherDisplay.innerHTML = `
          <div class="weather-main">
            <i class="weather-icon-large ${icon}"></i>
            <div class="weather-info">
              <div class="temperature">${Math.round(current.temperature)}°C</div>
              <div class="condition">${text}</div>
              <div class="location"><i class="ri-map-pin-line"></i> ${location}</div>
            </div>
          </div>
          <div class="weather-details">
            <div class="detail-item">
              <span class="detail-label">最高</span>
              <span class="detail-value">${Math.round(daily.temperature_2m_max[0])}°</span>
            </div>
            <div class="detail-item">
              <span class="detail-label">最低</span>
              <span class="detail-value">${Math.round(daily.temperature_2m_min[0])}°</span>
            </div>
            <div class="detail-item">
              <span class="detail-label">风速</span>
              <span class="detail-value">${current.windspeed}km/h</span>
            </div>
          </div>
        `;
      } catch (error) {
          weatherDisplay.innerHTML = `
          <div class="weather-error">
            <i class="ri-cloud-sun-line"></i>
            <p>天气服务暂时不可用</p>
          </div>
        `;
      }
    }
  })();
</script>

<style>
  .weather-card {
    position: relative;
    background: rgba(0, 0, 0, 0.1);
    border-radius: 24px;
    border: 1px solid rgba(255, 255, 255, 0.15);
    padding: 1.5rem;
    overflow: hidden;
  }

  .card-glow {
    position: absolute;
    top: -50%;
    left: -50%;
    width: 200%;
    height: 200%;
    background: radial-gradient(circle, rgba(16, 185, 129, 0.15) 0%, transparent 70%);
    opacity: 0.5;
    pointer-events: none;
  }

  .weather-header {
    margin-bottom: 1rem;
    padding-bottom: 0.8rem;
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
  }

  .weather-title {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 1.1rem;
    font-weight: 700;
    background: linear-gradient(135deg, #fff, #a7f3d0);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .title-icon {
    -webkit-text-fill-color: initial;
  }

  .weather-content {
    min-height: 150px;
  }

  .weather-display {
    width: 100%;
  }

  .weather-loading {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 2rem;
    color: rgba(255, 255, 255, 0.6);
  }

  .loading-spinner {
    width: 30px;
    height: 30px;
    border: 2px solid rgba(255, 255, 255, 0.1);
    border-top-color: #10b981;
    border-radius: 50%;
    animation: spin 1s linear infinite;
    margin-bottom: 0.5rem;
  }

  @keyframes spin {
    to { transform: rotate(360deg); }
  }
</style>

<style is:global>
  #weather-display .weather-main {
    display: flex;
    align-items: center;
    gap: 1rem;
    margin-bottom: 1rem;
  }

  #weather-display .weather-icon-large {
    font-size: 3rem;
  }

  #weather-display .weather-error i {
    font-size: 2rem;
    margin-bottom: 0.5rem;
  }

  #weather-display .weather-info {
    flex: 1;
  }

  #weather-display .temperature {
    font-size: 2rem;
    font-weight: 700;
    color: #fff;
    line-height: 1;
  }

  #weather-display .condition {
    font-size: 1rem;
    color: rgba(255, 255, 255, 0.8);
    margin-top: 0.2rem;
  }

  #weather-display .location {
    font-size: 0.85rem;
    color: rgba(255, 255, 255, 0.6);
    margin-top: 0.2rem;
  }

  #weather-display .weather-details {
    display: flex;
    justify-content: space-around;
    padding-top: 1rem;
    border-top: 1px solid rgba(255, 255, 255, 0.1);
  }

  #weather-display .detail-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.2rem;
  }

  #weather-display .detail-label {
    font-size: 0.75rem;
    color: rgba(255, 255, 255, 0.5);
  }

  #weather-display .detail-value {
    font-size: 0.9rem;
    color: rgba(255, 255, 255, 0.9);
    font-weight: 600;
  }

  #weather-display .weather-error {
    text-align: center;
    padding: 2rem;
    color: rgba(255, 255, 255, 0.6);
  }

  #weather-display .weather-error p:first-child {
    font-size: 2rem;
    margin-bottom: 0.5rem;
  }
</style>
